<Page x:Class="TourAppShengals.AddEditHotelPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TourAppShengals"
      mc:Ignorable="d" 
      Background="white"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AddEditHotelPage">
    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="Name">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Grid.Row" Value="0"/>
            <Setter Property="Grid.Column" Value="0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="CountStars">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Grid.Row" Value="1"/>
            <Setter Property="Grid.Column" Value="0"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="Country">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Grid.Row" Value="2"/>
            <Setter Property="Grid.Column" Value="0"/>
        </Style>
        <Style TargetType="TextBox" x:Key="TextBoxOne">
            <Setter Property="FontSize" Value="35"/>
            <Setter Property="Width" Value="550"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Text" Value="{Binding Name}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Grid.Row" Value="0"/>
            <Setter Property="Grid.Column" Value="1"/>
        </Style>
        <Style TargetType="TextBox" x:Key="TextBoxTwo">
            <Setter Property="FontSize" Value="35"/>
            <Setter Property="Text" Value="{Binding CountOfStar}"/>
            <Setter Property="Width" Value="550"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Grid.Row" Value="1"/>
            <Setter Property="Grid.Column" Value="1"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="ComboBoxOne">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Width" Value="550"/>
            <Setter Property="SelectedItem" Value="{Binding Country}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Grid.Row" Value="2"/>
            <Setter Property="Grid.Column" Value="1"/>
        </Style>
        <Style TargetType="Button" x:Key="BtnSave">
            <Setter Property="Content" Value="Сохранить"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="30"/>
            <Setter Property="Background" Value="white"/>
            <Setter Property="Width" Value="195"/>
            <Setter Property="Grid.Row" Value="3"/>
            <Setter Property="Grid.Column" Value="1"/>
            <Setter Property="FontSize" Value="30"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="120"/>
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource Name}">Название</TextBlock>
        <TextBlock Style="{StaticResource CountStars}">Количество Звезд</TextBlock>
        <TextBlock Style="{StaticResource Country}">Страна</TextBlock>
        <TextBox Text="{Binding Name}" Style="{StaticResource TextBoxOne}"/>
        <TextBox Style="{StaticResource TextBoxTwo}"/>
        <ComboBox x:Name="ComboCountries" DisplayMemberPath="Name" Style="{StaticResource ComboBoxOne}"/>
        <Button Style="{StaticResource BtnSave}" Click="SaveBtn_Click" Name="SaveBtn"/>
    </Grid>
</Page>
